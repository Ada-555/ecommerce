from django.urls import path
# Webhook endpoints for triggering emails can go here if needed.
# Currently emails are triggered via signals (order status changes) and
# directly from checkout views (confirmation).
